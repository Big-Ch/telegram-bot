# 🤖 Telegram Чат-Бот

Полнофункциональный Telegram бот с регистрацией пользователей, профилями и интерактивным меню.

## 📋 Возможности

- ✅ Приветственное сообщение для новых пользователей
- ✅ Сбор базовой информации (имя, возраст, город, интересы)
- ✅ Интерактивное меню с кнопками
- ✅ Просмотр и редактирование профиля
- ✅ База данных SQLite для хранения данных пользователей
- ✅ Статистика бота
- ✅ Система помощи

## 🚀 Быстрый старт

### 1. Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например, "My Awesome Bot")
4. Введите username для бота (должен заканчиваться на "bot", например, "my_awesome_bot")
5. **Сохраните полученный токен** - он понадобится для запуска бота

### 2. Установка на локальном компьютере

#### Windows

```bash
# Клонируйте проект или создайте новую папку
cd C:\Users\YourName\Desktop\mobiles

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env
copy .env.example .env

# Откройте .env и вставьте ваш токен от BotFather
notepad .env
```

#### Linux/macOS

```bash
# Перейдите в папку проекта
cd ~/mobiles

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env
cp .env.example .env

# Откройте .env и вставьте ваш токен от BotFather
nano .env
```

### 3. Настройка файла .env

Откройте файл `.env` и замените `your_bot_token_here` на токен, полученный от BotFather:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
ADMIN_ID=your_telegram_id
```

### 4. Запуск бота

```bash
# Убедитесь, что виртуальное окружение активировано
python bot.py
```

Если всё настроено правильно, вы увидите сообщение:
```
INFO - Бот запущен!
```

### 5. Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Нажмите "Start" или отправьте команду `/start`
4. Следуйте инструкциям бота для регистрации

## 🌐 Варианты хостинга бота (24/7 работа)

### Вариант 1: PythonAnywhere (Бесплатно)

**PythonAnywhere** предоставляет бесплатный тариф с ограничениями.

#### Инструкция:

1. Зарегистрируйтесь на [PythonAnywhere](https://www.pythonanywhere.com/)
2. Перейдите в раздел **"Files"** и загрузите файлы проекта
3. Откройте **"Bash"** консоль и выполните:

```bash
# Установите зависимости
pip3.10 install --user -r requirements.txt

# Создайте файл .env
nano .env
# Вставьте ваш токен и сохраните (Ctrl+X, Y, Enter)
```

4. Создайте файл для запуска в фоновом режиме:

```bash
nano start_bot.sh
```

Вставьте:
```bash
#!/bin/bash
python3.10 bot.py
```

Сохраните и сделайте исполняемым:
```bash
chmod +x start_bot.sh
```

5. Запустите бота:
```bash
nohup ./start_bot.sh &
```

**Важно:** Бесплатный тариф имеет ограничения. Для полноценной работы 24/7 потребуется платный аккаунт.

---

### Вариант 2: Heroku

**Heroku** - популярная платформа для деплоя приложений.

#### Инструкция:

1. Зарегистрируйтесь на [Heroku](https://www.heroku.com/)
2. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
3. Создайте файл `Procfile` в корне проекта:

```bash
echo "worker: python bot.py" > Procfile
```

4. Создайте файл `runtime.txt`:

```bash
echo "python-3.11.0" > runtime.txt
```

5. Инициализируйте Git и деплой:

```bash
# Инициализация Git
git init
git add .
git commit -m "Initial commit"

# Создание приложения на Heroku
heroku login
heroku create your-bot-name

# Установка переменных окружения
heroku config:set BOT_TOKEN=your_bot_token_here

# Деплой
git push heroku master

# Запуск worker
heroku ps:scale worker=1
```

---

### Вариант 3: VPS (Digital Ocean, AWS, etc.)

**Для продвинутых пользователей** - аренда виртуального сервера.

#### Пример для Ubuntu Server:

1. Подключитесь к серверу по SSH:

```bash
ssh root@your_server_ip
```

2. Установите Python и зависимости:

```bash
apt update
apt install python3 python3-pip python3-venv git -y
```

3. Клонируйте проект:

```bash
cd /opt
git clone https://github.com/yourusername/your-bot-repo.git bot
cd bot
```

4. Создайте виртуальное окружение и установите зависимости:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

5. Настройте .env:

```bash
nano .env
# Вставьте токен и сохраните
```

6. Создайте systemd сервис для автозапуска:

```bash
nano /etc/systemd/system/telegrambot.service
```

Вставьте:
```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/bot
Environment="PATH=/opt/bot/venv/bin"
ExecStart=/opt/bot/venv/bin/python /opt/bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

7. Запустите сервис:

```bash
systemctl daemon-reload
systemctl enable telegrambot
systemctl start telegrambot

# Проверка статуса
systemctl status telegrambot
```

---

### Вариант 4: Railway.app (Рекомендуется)

**Railway** - современная платформа с бесплатным тарифом.

#### Инструкция:

1. Зарегистрируйтесь на [Railway.app](https://railway.app/)
2. Подключите GitHub аккаунт
3. Создайте репозиторий на GitHub и загрузите код
4. На Railway нажмите **"New Project"** → **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий
6. Добавьте переменные окружения:
   - Перейдите в **Variables**
   - Добавьте `BOT_TOKEN` с вашим токеном
7. Railway автоматически развернет бота

Для корректной работы создайте `Procfile`:
```
worker: python bot.py
```

---

### Вариант 5: Replit (Самый простой)

**Replit** - онлайн IDE с возможностью хостинга.

#### Инструкция:

1. Зарегистрируйтесь на [Replit](https://replit.com/)
2. Создайте новый Repl (Python)
3. Загрузите файлы проекта
4. В разделе **Secrets** (замок слева) добавьте:
   - Key: `BOT_TOKEN`
   - Value: ваш токен от BotFather
5. Измените `bot.py` для работы с Secrets:

```python
from os import getenv
import os

# Для Replit
TOKEN = os.environ.get("BOT_TOKEN") or getenv("BOT_TOKEN")
```

6. Нажмите кнопку **Run**

**Важно:** Для работы 24/7 на Replit используйте сервис [UptimeRobot](https://uptimerobot.com/) для поддержания активности.

---

## 📝 Структура проекта

```
mobiles/
├── bot.py                 # Основной файл бота
├── database.py            # Модуль работы с базой данных
├── requirements.txt       # Зависимости Python
├── .env                   # Конфигурация (токен бота)
├── .env.example           # Пример конфигурации
├── .gitignore            # Игнорируемые файлы для Git
├── README.md             # Документация
└── bot_database.db       # База данных SQLite (создается автоматически)
```

## 🎯 Команды бота

- `/start` - Начать работу с ботом / Регистрация
- `/help` - Показать справку
- `/menu` - Открыть главное меню

## 🔧 Кнопки меню

- **📋 Мой профиль** - Просмотр информации профиля
- **✏️ Редактировать профиль** - Обновление данных
- **ℹ️ Информация** - Информация о боте
- **❓ Помощь** - Справка по командам
- **📊 Статистика бота** - Общая статистика

## 🛠️ Технологии

- **Python 3.10+**
- **aiogram 3.3.0** - Асинхронная библиотека для Telegram Bot API
- **aiosqlite** - Асинхронная работа с SQLite
- **python-dotenv** - Управление переменными окружения

## 📊 База данных

Бот использует SQLite базу данных для хранения информации о пользователях:

### Таблица users

| Поле | Тип | Описание |
|------|-----|----------|
| user_id | INTEGER | Telegram ID пользователя (первичный ключ) |
| username | TEXT | Username в Telegram |
| name | TEXT | Имя пользователя |
| age | INTEGER | Возраст |
| city | TEXT | Город |
| interests | TEXT | Интересы/хобби |
| created_at | TIMESTAMP | Дата регистрации |
| updated_at | TIMESTAMP | Дата последнего обновления |

## 🔒 Безопасность

- ⚠️ **Никогда не публикуйте файл `.env` с токеном в открытом доступе**
- ⚠️ Добавьте `.env` в `.gitignore`
- ⚠️ Используйте `.env.example` как шаблон для других разработчиков

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте правильность токена в `.env`
2. Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
3. Проверьте версию Python: `python --version` (должна быть 3.10+)

### Бот не отвечает

1. Убедитесь, что бот запущен
2. Проверьте логи в консоли на наличие ошибок
3. Убедитесь, что бот не заблокирован в Telegram

### Ошибки базы данных

1. Удалите файл `bot_database.db` и перезапустите бота
2. База данных создастся автоматически

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи в консоли
3. Убедитесь, что используете последнюю версию зависимостей

## 📜 Лицензия

Этот проект создан в образовательных целях и может быть свободно использован и модифицирован.

## 🎉 Дополнительные возможности для расширения

- Добавление административной панели
- Рассылка сообщений пользователям
- Интеграция с внешними API
- Добавление платежных систем
- Создание опросов и голосований
- Многоязычность
- Аналитика и отчеты

---

**Создано с ❤️ для Telegram сообщества**

