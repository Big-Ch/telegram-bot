# 🚀 Подробная инструкция по хостингу Telegram бота

## 📋 Содержание

1. [Получение токена бота](#1-получение-токена-бота)
2. [Локальный запуск для тестирования](#2-локальный-запуск-для-тестирования)
3. [Бесплатный хостинг на Replit](#3-бесплатный-хостинг-на-replit)
4. [Бесплатный хостинг на Railway](#4-бесплатный-хостинг-на-railway)
5. [VPS хостинг (профессиональный вариант)](#5-vps-хостинг)

---

## 1. Получение токена бота

### Шаг 1: Открыть BotFather

1. Откройте Telegram на телефоне или компьютере
2. В поиске найдите **@BotFather** (у него синяя галочка)
3. Откройте чат с BotFather

### Шаг 2: Создать нового бота

1. Отправьте команду: `/newbot`
2. BotFather спросит имя бота. Введите, например: `Мой Классный Бот`
3. Затем введите username бота (должен заканчиваться на "bot"), например: `my_cool_bot`
4. **Важно!** Сохраните полученный токен, он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Шаг 3: Настройка бота (опционально)

```
/setdescription - Установить описание бота
/setabouttext - Текст "О боте"
/setuserpic - Установить фото профиля
/setcommands - Настроить команды для меню
```

Пример команд для `/setcommands`:
```
start - Начать работу с ботом
help - Получить помощь
menu - Открыть главное меню
```

---

## 2. Локальный запуск для тестирования

### Для Windows

#### Шаг 1: Установить Python

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** поставьте галочку "Add Python to PATH"
3. Проверьте установку, открыв командную строку (Win+R → `cmd`):
   ```bash
   python --version
   ```

#### Шаг 2: Подготовить проект

1. Откройте папку проекта в проводнике
2. В адресной строке введите `cmd` и нажмите Enter
3. Выполните команды:

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

#### Шаг 3: Настроить токен

1. Найдите файл `.env.example`
2. Скопируйте его и переименуйте в `.env`
3. Откройте `.env` блокнотом
4. Замените `your_bot_token_here` на ваш токен от BotFather
5. Сохраните файл

#### Шаг 4: Запустить бота

```bash
python bot.py
```

Вы должны увидеть:
```
INFO - Бот запущен!
```

Теперь откройте Telegram и найдите вашего бота!

---

### Для Linux/Mac

#### Шаг 1: Установить Python (если не установлен)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip python3-venv

# macOS (с Homebrew)
brew install python3
```

#### Шаг 2: Подготовить проект

```bash
# Перейти в папку проекта
cd ~/mobiles

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

#### Шаг 3: Настроить токен

```bash
# Создать файл .env из примера
cp .env.example .env

# Открыть для редактирования
nano .env
# Вставьте токен, затем Ctrl+X, Y, Enter для сохранения
```

#### Шаг 4: Запустить бота

```bash
python3 bot.py
```

---

## 3. Бесплатный хостинг на Replit

**Replit** - самый простой способ для начинающих!

### Шаг 1: Регистрация

1. Перейдите на [replit.com](https://replit.com/)
2. Нажмите "Sign up"
3. Зарегистрируйтесь через Google или GitHub

### Шаг 2: Создание проекта

1. Нажмите "+ Create Repl"
2. Выберите "Python" как язык
3. Назовите проект, например, "telegram-bot"
4. Нажмите "Create Repl"

### Шаг 3: Загрузка файлов

1. В левой панели найдите кнопку "Upload file"
2. Загрузите все файлы проекта:
   - `bot.py`
   - `database.py`
   - `requirements.txt`

### Шаг 4: Установка зависимостей

В консоли Replit выполните:
```bash
pip install -r requirements.txt
```

### Шаг 5: Добавление токена

1. В левой панели найдите иконку замка (🔒) "Secrets"
2. Нажмите "+ New Secret"
3. Key: `BOT_TOKEN`
4. Value: вставьте ваш токен от BotFather
5. Нажмите "Add Secret"

### Шаг 6: Изменение кода для Replit

Откройте `bot.py` и измените строку с токеном:

```python
import os
from dotenv import load_dotenv

load_dotenv()
# Для Replit используем os.environ
TOKEN = os.environ.get("BOT_TOKEN") or getenv("BOT_TOKEN")
```

### Шаг 7: Запуск

1. Нажмите зеленую кнопку "Run" вверху
2. Бот запустится!

### Шаг 8: Поддержание работы 24/7

Replit останавливает бесплатные проекты при неактивности. Решение:

1. Зарегистрируйтесь на [uptimerobot.com](https://uptimerobot.com/)
2. Создайте новый монитор типа "HTTP(s)"
3. URL: скопируйте URL вашего Replit проекта
4. Интервал: 5 минут
5. UptimeRobot будет "пинговать" ваш проект, сохраняя его активным

**Альтернатива:** Оформите Replit Hacker Plan за $7/месяц для Always On.

---

## 4. Бесплатный хостинг на Railway

**Railway** - современная платформа с удобным деплоем.

### Шаг 1: Подготовка репозитория на GitHub

1. Зарегистрируйтесь на [github.com](https://github.com/)
2. Создайте новый репозиторий "telegram-bot"
3. Загрузите файлы проекта

**Через Git:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/Big-Ch/telegram-bot.git
git push -u origin master
```

### Шаг 2: Создание Procfile

Создайте файл `Procfile` (без расширения) в корне проекта:

```
worker: python bot.py
```

Добавьте в Git:
```bash
git add Procfile
git commit -m "Add Procfile"
git push
```

### Шаг 3: Регистрация на Railway

1. Перейдите на [railway.app](https://railway.app/)
2. Нажмите "Login" → "Login with GitHub"
3. Авторизуйте Railway доступ к GitHub

### Шаг 4: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий "telegram-bot"
4. Railway автоматически начнет деплой

### Шаг 5: Добавление переменных окружения

1. Откройте проект
2. Перейдите во вкладку "Variables"
3. Нажмите "New Variable"
4. Добавьте:
   - **Variable:** `BOT_TOKEN`
   - **Value:** ваш токен от BotFather
5. Сохраните

### Шаг 6: Проверка

Railway автоматически перезапустит проект. Проверьте логи во вкладке "Deployments".

**Готово!** Бот работает 24/7 бесплатно (с лимитом 500 часов в месяц на бесплатном тарифе).

---

## 5. VPS хостинг

**Для опытных пользователей** - полный контроль и надежность.

### Рекомендуемые провайдеры

- **Digital Ocean** - от $4/мес ([referral link](https://www.digitalocean.com/))
- **Vultr** - от $2.50/мес
- **Hetzner** - от €4/мес (очень выгодно)
- **Timeweb** - от 149₽/мес (русский хостинг)

### Инструкция для Ubuntu 22.04

#### Шаг 1: Подключение к серверу

```bash
ssh root@your_server_ip
# Введите пароль, полученный от хостинга
```

#### Шаг 2: Обновление системы

```bash
apt update && apt upgrade -y
```

#### Шаг 3: Установка Python и зависимостей

```bash
apt install python3 python3-pip python3-venv git nano -y
```

#### Шаг 4: Создание пользователя для бота (рекомендуется)

```bash
adduser botuser
usermod -aG sudo botuser
su - botuser
```

#### Шаг 5: Загрузка проекта

**Вариант A: Через Git**
```bash
cd ~
git clone https://github.com/ваш-username/telegram-bot.git bot
cd bot
```

**Вариант B: Загрузка файлов через SFTP**
Используйте FileZilla или WinSCP для загрузки файлов.

#### Шаг 6: Установка зависимостей

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### Шаг 7: Настройка .env

```bash
nano .env
```

Вставьте:
```env
BOT_TOKEN=ваш_токен_здесь
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

#### Шаг 8: Проверка запуска

```bash
python3 bot.py
```

Если всё работает, нажмите `Ctrl+C` для остановки.

#### Шаг 9: Настройка автозапуска с systemd

Выйдите из пользователя botuser:
```bash
exit
```

Создайте systemd service:
```bash
nano /etc/systemd/system/telegrambot.service
```

Вставьте:
```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/bot
Environment="PATH=/home/botuser/bot/venv/bin"
ExecStart=/home/botuser/bot/venv/bin/python3 /home/botuser/bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните файл.

#### Шаг 10: Запуск и включение автозапуска

```bash
# Перезагрузить конфигурацию systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable telegrambot

# Запустить бота
systemctl start telegrambot

# Проверить статус
systemctl status telegrambot
```

#### Полезные команды для управления

```bash
# Остановить бота
systemctl stop telegrambot

# Перезапустить бота
systemctl restart telegrambot

# Посмотреть логи
journalctl -u telegrambot -f

# Посмотреть последние 100 строк логов
journalctl -u telegrambot -n 100
```

#### Шаг 11: Настройка файрвола (опционально, но рекомендуется)

```bash
# Установить UFW
apt install ufw

# Разрешить SSH
ufw allow 22/tcp

# Включить файрвол
ufw enable

# Проверить статус
ufw status
```

### Обновление бота на VPS

```bash
# Подключиться к серверу
ssh botuser@your_server_ip

# Перейти в папку проекта
cd ~/bot

# Получить обновления
git pull

# Активировать venv
source venv/bin/activate

# Обновить зависимости (если изменились)
pip install -r requirements.txt

# Выйти из venv
deactivate

# Перезапустить бота
sudo systemctl restart telegrambot
```

---

## 🔍 Проверка работы бота

После запуска на любой платформе:

1. Откройте Telegram
2. Найдите вашего бота по username
3. Нажмите "Start" или отправьте `/start`
4. Пройдите регистрацию
5. Протестируйте все кнопки меню

---

## ⚠️ Важные советы

### Безопасность

- ✅ **НИКОГДА** не публикуйте токен бота в открытом доступе
- ✅ Используйте `.gitignore` для исключения `.env` из Git
- ✅ Регулярно обновляйте зависимости: `pip install --upgrade -r requirements.txt`
- ✅ Делайте резервные копии базы данных

### Мониторинг

- 📊 Регулярно проверяйте логи
- 📊 Используйте команду `/stats` (если добавили) для мониторинга активности
- 📊 На VPS настройте мониторинг ресурсов

### Резервное копирование

Регулярно сохраняйте базу данных:

```bash
# Локально
cp bot_database.db bot_database_backup_$(date +%Y%m%d).db

# На VPS
scp botuser@your_server_ip:~/bot/bot_database.db ./backup/
```

---

## 🆘 Решение проблем

### Бот не отвечает

1. Проверьте, запущен ли процесс
2. Проверьте логи на наличие ошибок
3. Убедитесь, что токен правильный
4. Проверьте интернет-соединение сервера

### Ошибка "Chat not found"

- Убедитесь, что вы нажали "Start" в боте

### Ошибка импорта модулей

```bash
# Переустановите зависимости
pip install --force-reinstall -r requirements.txt
```

### База данных заблокирована

```bash
# Перезапустите бота
sudo systemctl restart telegrambot
```

---

## 📞 Дополнительная помощь

### Полезные ресурсы

- [Официальная документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Документация aiogram](https://docs.aiogram.dev/)
- [Python документация](https://docs.python.org/3/)

### Сообщества

- [Aiogram чат в Telegram](https://t.me/aiogram_ru) - русскоязычное сообщество
- [Stack Overflow](https://stackoverflow.com/questions/tagged/telegram-bot)

---

**Удачи в запуске вашего бота! 🚀**

